# MyLocalTube（youtube-ui-player）— 開発メモ

使い方は `README.md`。このファイルは仕様・設計判断・実装の要点・踏んだ不具合の記録。

## 1. 役割

手元の動画フォルダを **動かさず・コピーせず** に索引し、YouTube 風の画面で再生する Windows 向けアプリ。
任意で、外部の 120fps 変換ツールへ変換ジョブを投げられる。

```
MyLocalTube.exe（ランチャ）→ pythonw desktop.py
  ├── WebView2 のウィンドウ（static/index.html。ビルド不要の単一 HTML）
  └── FastAPI サーバ（同じプロセスの別スレッド。127.0.0.1 のみ）
        ├── library.py … 走査・索引（SQLite）・コーデック判定・サムネイル
        ├── app.py     … 動画の Range 配信 / 詰め替え配信、API、進捗の SSE
        ├── fileops.py … フォルダへ移動・ごみ箱へ移動・エクスプローラーで表示
        └── jobs.py    … 120fps 変換ジョブのキュー（外部ツールを子プロセスで実行）

osdeps.py … OS ごとに違う処理の窓口（desktop / fileops / jobs はここだけを通す）
  ├── platform_win.py … Windows: Win32 API・ごみ箱・エクスプローラー・taskkill
  └── platform_mac.py … macOS: まだ空の実装（呼ばれたことをログに残すだけ）
```

動画はパスを DB に記録するだけで、ファイルが動くのは ⋮ メニューで操作したときだけ。

## 2. 現在の問題点（未解決）

| # | 内容 |
|---|---|
| 1 | Windows 専用（WebView2・Win32 API・ごみ箱・レジストリを使う）。macOS 版は OS ごとの処理を分けたところまで（§4.5。macOS 側は空の実装） |
| 2 | MPEG-2 映像の .ts は WebView2 で再生できないので一覧に出していない。見たい場合はリアルタイム変換（NVENC 等）が必要 |
| 3 | 変換ツールを強制終了すると、ツールの作業フォルダが残ることがある（ツール側で起動時に掃除するのが筋） |
| 4 | 最大化中はウィンドウを動画の縦横比に合わせられないので、画面と比率が違うと細い黒帯が出る |
| 5 | タスクバーへのピン留めは、起動中のウィンドウからではなくショートカットから行う必要がある |

## 3. 設計判断

| 項目 | 判断 | 理由 |
|---|---|---|
| バックエンド | Python + FastAPI | 変換ツール（Python）を子プロセスで扱いやすい |
| フロントエンド | ビルド不要の単一 HTML | 個人用ツールにビルドパイプラインは重い |
| DB | SQLite | 索引だけなので十分 |
| アプリ化 | pywebview（WebView2）＋ 19KB の C# ランチャ | PyInstaller で固めると、コード変更のたびに再ビルドが要り、出力フォルダ内の `data/` が消える |
| 再生可否 | **映像コーデック**で判定（`ffmpeg -i` のヘッダ表示を読む） | 拡張子だと、H.264 の .ts（再生できる）を消し、壊れた .mp4（再生できない）を残してしまう |
| 削除 | ごみ箱へ（`SHFileOperationW` + `FOF_ALLOWUNDO`） | 元に戻せる。ごみ箱に入らないときは `FOF_WANTNUKEWARNING` で Windows に警告を出させる |
| 移動 | 同じドライブ内の `os.rename` のみ | 数GBでも一瞬で終わる。別ドライブはコピーになるので対象外。上書きはしない |
| 120fps 変換 | 外部ツール（任意） | 変換ツールは本リポジトリに含めない。仕様は README |
| 設定 | `config.py`（既定値）＋ `config_local.py`（個人の値。git 管理外） | 動画フォルダの場所などをリポジトリに入れない |
| OS ごとの処理 | `osdeps.py` が `platform_win.py` / `platform_mac.py` の一方を選ぶ | `sys.platform` の分岐を散らさず、Windows の動作を変えずに macOS を足すため。名前を `platform.py` にしない（標準ライブラリの `platform` を隠す） |

## 4. 実装の要点

### 4.1 動画の配信（app.py `media()`）

- mp4 / m4v / webm は HTTP Range で配信（シーク可）。mkv 等は ffmpeg で MP4 へ詰め替えて配信（シーク不可）
- **Starlette の `FileResponse` は使わない。** uvicorn（ASGI spec 2.3）は切断後の送信を黙って捨てるため、
  FileResponse は切断に気づかずファイルの最後まで読み続けてから閉じる（シークのたびに残りを空読みし、その間は移動も削除もできない）
- 自前の Range ジェネレータを StreamingResponse（切断を監視する）で流し、`_ClosingStream` で
  送り終わり・切断のどちらでも必ず閉じる（Starlette は切断時に中身のジェネレータを閉じないため）

### 4.2 アプリ化（desktop.py）

- 名前付きミューテックスで二重起動を防ぐ。起動済みならそのウィンドウを前面に出し、
  渡されたファイルは `data/desktop.port` のポートの `/api/open` へ渡して終わる
- すべての外部コマンドに `CREATE_NO_WINDOW`（pythonw から起動すると子ごとに黒いコンソール窓が開くため）
- WebView2 は要素の全画面化がウィンドウの枠内で止まるので、`fullscreenchange` を受けてウィンドウも全画面にする
- 動画の縦横比にウィンドウの表示領域を合わせ（黒帯を出さない）、一覧に戻ると元の大きさに戻す。
  サイズは Win32 で実ピクセルのまま扱う（pywebview の resize は DPI 換算が入る）

### 4.3 操作バー（index.html「プレイヤーの操作バー（YouTube 風）」）

- カーソルを動かすと表示し、再生中は 2.5 秒で隠す（一時停止中・操作バー上・メニュー表示中・エラー中は隠さない）
- Chrome は表示を切り替えただけで座標の変わらない mousemove を送るので、座標が変わったときだけ反応する
- 音量ブーストは Web Audio（Gain → DynamicsCompressor をリミッターとして）。一度つなぐと
  その `<video>` の音はずっと Web Audio 経由になる（仕様上外せない）。ホイールはユーザー操作扱いにならないので、
  AudioContext が停止状態で作られたら次のクリック／キー入力で再開する
- 再生速度・音量ブーストはボタンの上のホイールで変更。キー操作は YouTube と同じ割り当て

### 4.4 既定のアプリ（launcher/file_assoc.py）

- HKCU に ProgID・OpenWithProgids・Capabilities・RegisteredApplications を登録し、
  `ms-settings:defaultapps?registeredAppUser=MyLocalTube` を開く
- 既定（UserChoice）そのものはハッシュで保護されていて、アプリからは変えられない。最後の選択は人の操作

### 4.5 OS ごとの処理（osdeps.py / platform_win.py / platform_mac.py）

- `platform_*.py` には OS の API を呼ぶ薄い部品だけを置く（ミューテックス・ダイアログ・ウィンドウの位置と大きさ・
  ごみ箱・フォルダで表示・プロセスツリーの停止）。画面に出す文言・ログ・判断（何秒待つか、失敗したらどうするか）は
  呼び出し側（desktop.py / fileops.py / jobs.py）に残す。2 つのファイルの関数の名前と引数はそろえる
- `platform_mac.py` はまだ空の実装。呼ばれるたびに `phase=platform fn=<名前> impl=stub ms=` をログに残す。
  成功したふりはしない（ごみ箱へ送る・Finder で表示は `NotImplementedError`）。`window_handle()` が 0 を返すので、
  ウィンドウを動画に合わせる処理などは何もしない
- 起動時に `phase=platform name= os= arch= python= pywebview=` をログに残す（Mac で調べるときの手がかり）
- 分けたときの確認: 分ける前と後で同じ回帰テスト（移動・ごみ箱・二重起動・起動中への受け渡し・ダイアログ・
  ウィンドウ合わせ・全画面・変換の取り消し・exe の再起動）を流し、`desktop.log` の処理の並びが同じことを確かめた

## 5. 踏んだ不具合と対策（同じ轍を踏まないため）

| # | 症状 | 原因 | 対策 |
|---|---|---|---|
| a | ジョブ実行中に `/api/jobs` が 500 | ジョブ辞書の Popen を JSON 化しようとした | `_public()` で `_` 始まりの内部キーを除く |
| b | 初回走査の間ずっと一覧が空 | 最後に一括コミットしていた | 50件ごとにコミット |
| c | 変換の進捗が途中で止まって見える | 変換ツールの進捗表示の2形式（件数型・割合型）の片方しか解析していなかった | 両方を解析 |
| d | MPEG-2 映像の .ts が再生すらできない | MPEG-2 は WebView2 でデコードできない。詰め替えてもコーデックは変わらない | コーデック判定で一覧から外す |
| e | アプリ版でサムネイルのたびに黒い窓 | pythonw から console アプリを起動するとコンソールが作られる | `CREATE_NO_WINDOW` |
| f | 閉じてすぐ起動し直すと何も開かない | Python 3.12 の asyncio はサーバ停止時に全接続の終了を待つ。開きっぱなしの SSE で約5秒停止できず、その間ミューテックスが残った | 終了時に SSE を自分で閉じる（停止 5,007ms → 145ms）＋前回分の終了を待って起動 |
| g | 変換の取り消しで、途中までの出力が「変換済み」として残る | `terminate()` は親しか止めず、子の ffmpeg が入力の終わりとみなして書き終えていた | `taskkill /T /F` でツリーごと止め、このジョブが作った出力を消す |
| h | 一度再生した動画をしばらく移動・削除できない | §4.1 の配信の切断処理 | §4.1。切断→解放 20〜40ms |
| i | ごみ箱への移動で最大11秒待たされる | `SHFileOperationW` は使用中ファイルに対して内部で約1秒待つ。再試行を回数で区切っていた | 時間（4秒）で区切る |
| j | 起動直前の取り消しで、完成した変換が消える | status=running と Popen の保存の間に取り消しが来ると、止める相手がいない | Popen 保存直後に取り消しを再確認 |
| k | 移動後に索引更新が失敗すると、ファイルと索引の場所がずれる | 巻き戻しがファイル移動しか見ていなかった | 索引更新まで含めて巻き戻す |
| l | メニューを閉じるクリックで再生/停止まで切り替わる | 外側クリックで閉じる処理が先に走り、動画のクリックが素通りした | 閉じた直後のクリックは再生切り替えに使わない |
| m | 全画面のまま「←」で戻るとウィンドウの大きさが戻らない | 全画面解除時に pywebview が全画面前の大きさで上書きする | 全画面中の「戻す」は解除後まで保留 |
| n | 変換完了後も「変換済み」にならない | 完了通知が索引更新より先に届いていた | 索引更新後にもう一度通知 |

## 6. バグ修正・改修の手順

一気に直さない。各ステップの結果を確認してから次へ進む。

1. **Step 1: 調査とログ追加（見える化）** — 修正前に、どこで何ms・何が失敗しているかを数値で把握する。
   ログは `data/desktop.log`（追跡用 run ID・処理名 `phase=`・所要時間 `ms=`・成否 `ok=` を含む）
2. **Step 2: 原因箇所のみ最小限の改修** — 編集前にバックアップ。関係ない箇所は触らない
3. **Step 3: 周辺の整合性確認** — 同じ手順で再現しないことを確かめる。旧コードは確認が済むまで残す

計測の注意:
- 長時間処理はバックグラウンドで動かし、進捗はファイル側で追う（パイプは出力がバッファされて見えない）
- 計測中に同じドライブを走査しない（計測が汚れる）

## 7. 検証のやり方

- **本物のライブラリでは試さない。** テスト用スクリプトで `config.LIBRARY_ROOTS` / `DATA_DIR` を
  一時フォルダに差し替え、短い動画のコピー（大きい動画はハードリンク）で移動・削除・配信を試す
- アプリ版の確認は、ウィンドウ名・ミューテックス名・ポートをテスト用に差し替えた別インスタンスで行う
  （使用中のアプリと混ざらないように）
- 画面の確認は WebView2 の開発者ツール用プロトコル（`webview.settings["REMOTE_DEBUGGING_PORT"]`）か、
  ブラウザで `start.cmd` 版を開いて行う。音は出さない（`V.muted = true`、音量ブーストはスピーカーへの経路を外して計測）
- テストで出るダイアログを自動で閉じるときは、ボタンを `BM_CLICK` で押す。OK だけのメッセージボックスの
  OK ボタンの ID は 2 なので、`WM_COMMAND`（IDOK=1）を送っても閉じない
- 開発者ツール用プロトコルで送った Escape では WebView2 の全画面は解除されない。解除の確認は `f` キーで行う
- venv の `python.exe` は本体（元の Python の `python.exe`）を子プロセスとして起動するランチャ。
  プロセスを数えるテストでは 1 回の起動が 2 つに見える（ランチャを止めれば本体も止まる）

## 8. 関連ドキュメント

- `README.md` — 使い方・セットアップ
- `launcher/DEVELOPMENT.md` — ランチャ（exe）と既定のアプリ登録
